import numpy as np
from scipy.optimize import minimize

def obj_func(engine_rating, source_list):
    fuel_burn = 0

    # assign engine_rating to each source in source_lost
    # create engine_loading instance

